int PARAM = 5;

int myfun(int a, int b)
{
	bool my_bool = a>b;
	return a + b;
}


int main() 
{
	
	int a = 0;
	while(false)
	{
		{
			int d = 0;
		}
		int b = 0xFFFF + 0X0001 % 0123 - 4;
		int c;
		c = 36;
		
		if(a > b)
		{
			if(a < b) return myfun(a,b);
			else return 2;
		}
		a = a * (b / c);
	}

	if(false || (true && false))
	{
		while(false^false);
		bool b = true;
		b = b & b | b;
		int d = 0;
	}
	else
	{

	}
  return 0+0;
}