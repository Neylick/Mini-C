int PARAM = 5;

int myfun(int a, int b)
{
	bool my_bool = a>b;
	return a + b;
}

// Comment can also be out of function
/* Also /*works*/ with blocks, even with multiple layered ones */


int main() 
{
	/* 
		THIS IS A COMMENT	
	*/
	// THIS IS ALSO A COMMENT
	
	int a = 0;
	while(false)
	{
		// Custom scope with local variables
		{
			int d = 0;
		}
		int d = 2;
		int b = 0xFFFF + 0X0001 % 0123 - 4;
		int c;
		c = 36;
		
		if(a > b)
		{
			if(a < b) return myfun(a,b);
			else return 2;
		}
		a = a * (b / c);
	}

	/* OK */
	// Ok
	if(false || (true && false))
	{
		while(false^false); //while syntax
		bool b = true; //Redefining variables in different scopes
		b = b & b | b;
		int d = 0;
	}
	else
	{

	}
  return 0+0;
}

void afficher()
{
	// We could do stuff here
}